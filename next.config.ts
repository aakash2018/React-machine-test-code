import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // output: 'export',
  // trailingSlash: true,
  // basePath: "/my-local-bucket",
  // assetPrefix: "/my-local-bucket",
};

export default nextConfig;
